[package]
name = "starter_attr"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
hotaru = { path = "../../hotaru", version = "=0.8.4", default-features = false, features = ["attr", "auto-reg", "http", "tokio", "full_regex", "template"] }
